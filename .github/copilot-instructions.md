# Explorer UI Library Instructions

- Keep the project focused on reusable UI components for MongoDB-style data exploration.
- Prefer small, stateless components with explicit props over global state.
- Preserve the library structure in `src/` and avoid coupling the components to the demo app.
- Keep Tailwind utility usage consistent and avoid adding unnecessary dependencies.
- Treat realtime/socket support as an integration point, not as a hard requirement.
